h1 Редактирование товара

- flash.each do |k, v|
  p class="flash #{k}"= v

.main_content
  h2= @resource.title

  p= link_to 'Ссылка на сайт производителя', @resource.full_url, target: :_blank

  ul.images= render collection: @resource.proxied_images.first(5), partial: 'product_image', as: :image_url

.side_bar
  = link_to 'Назад к товарам без цвета', admin_products_path(filter_in: :no_color)
  br
  = link_to 'Назад к платьям без длины', admin_products_path(filter_in: :no_length)
  = render 'form', product: @resource
